import styled from "styled-components";

const Consumer = () => {
  return (
    <ConsumerContainer>
    </ConsumerContainer>
  );
};

export default Consumer;

const ConsumerContainer = styled.div`

`;
